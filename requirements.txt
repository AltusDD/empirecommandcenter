azure-functions==1.19.0
pg8000==1.31.2
httpx
python-dotenv
requests-toolbelt
# Dropbox SDK
dropbox
# Optional; using REST via httpx in code, but you can swap to client usage later
supabase
azure-functions
requests==2.32.3

